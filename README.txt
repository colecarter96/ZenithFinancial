Things to install:
    pip install beautifulsoup4
    brew install python              #if brew is already installed
    pip install requests
    pip3 install alpaca-py

make sure to:
`cd ZenithFinancial`
then should be able to just call:
`pip install -r requirements.txt`
at least for mac, might need:
`pip3 install -r requirements.txt`
